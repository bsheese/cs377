# 17.2 · Multiple Linear Regression

## 17_2_4_1: Data Cleaning

### In data cleaning, what is the primary conceptual reason for removing 'extreme outliers' (like very large houses sold for very low prices) before building a linear model?
- [ ] To increase the total size of the dataset used for training
- [x] To prevent a few unusual observations from disproportionately skewing the regression line
- [ ] To ensure the model only learns from luxury home price examples
- [ ] Because linear regression cannot process very large numerical input values

### Why is it conceptually important to remove unique identifiers like 'PID' or 'Order' from a machine learning featureset?
- [ ] They are too large to store efficiently in available memory
- [ ] They are string columns and linear regression only accepts numeric input
- [x] They provide no generalizable information and could lead to memorizing specific rows
- [ ] They are perfectly correlated with the target variable by construction

### What is the statistical problem with a 'monotonic' feature (a column where every single value is the same)?
- [x] It has zero variance and therefore provides no predictive information
- [ ] It will cause the model to crash due to a division-by-zero error
- [ ] It makes the R-squared value unrealistically high
- [ ] It is considered a structurally dependent or 'yolked' variable

### The notebook describes 'yolked features' (e.g., Pool Quality and Pool Area). What is the primary concern when these two features are included in a model together?
- [x] They represent redundant information where one value is structurally dependent on the other
- [ ] They are guaranteed to have missing values at the exact same rows
- [ ] They are measured in incompatible units that cannot be combined in a model
- [ ] They cause the target variable to become right-skewed during training

### If a house has 'None' for a basement category and '0' for basement square footage, what does this tell us about the relationship between these features?
- [ ] The data entry for this row is corrupted or incorrect
- [x] The categorical 'None' is a structural prerequisite for the numerical '0'
- [ ] The two variables are statistically independent of each other
- [ ] One feature is an outlier relative to the distribution of the other

### Why would a data scientist choose to drop a feature like 'Pool Area' if 99% of the entries are zero?
- [ ] Because zeros cannot be processed by a linear regression model
- [x] To reduce noise and complexity in the model for a very small gain in predictive power
- [ ] Because the model only generates valid predictions for houses with pools
- [ ] To ensure the training and test datasets are perfectly balanced

### In the context of 'Garage Yr Blt', why is it dangerous to simply replace missing values with '0'?
- [x] A year of '0' acts as a massive outlier that distorts the temporal logic of the model
- [ ] Because Python does not recognize 0 as a valid calendar year value
- [ ] Because it makes the house appear newer than its actual construction date
- [ ] Because 0 is a value reserved exclusively for categorical feature encoding

### What is the conceptual goal of 'collapsing' a categorical variable with 10 categories into just 'Category A' and 'Other'?
- [ ] To increase the total number of one-hot encoded features in the model
- [x] To simplify the model and prevent sparsity from too many near-empty columns
- [ ] To make the feature more difficult for students to interpret
- [ ] To ensure the regression decision boundary becomes non-linear

### Why do we check for 'unbalanced' categoricals (e.g., where 95% of houses are on a 'Paved Street')?
- [ ] To find the most popular housing type in the dataset
- [x] Because variables with very little variance provide almost no signal for the model to learn from
- [ ] Because linear regression requires a 50/50 class split in all categorical features
- [ ] To identify which house characteristics are most expensive on average

### Consider the code: `df['garage_attached'] = np.where(df['Garage Type'] == 'Attchd', 1, 0)`. What is the logical purpose of this transformation?
- [ ] To delete the original 'Garage Type' column from the DataFrame
- [x] To create a binary indicator variable focusing on one specific attribute
- [ ] To compute the average sale price of houses with attached garages
- [ ] To normalize the garage area to a standard unit scale

### What is the primary benefit of converting binary string columns (like 'Yes'/'No') into the 'boolean' or 'bool' data type?
- [ ] It reduces the file size of the saved dataset on disk
- [x] It clearly communicates to the model and programmer that the data is a logical toggle
- [ ] It allows the model to handle missing values in that column automatically
- [ ] It prevents the column from being used in correlation heatmap visualizations

### In the code `df.dropna(how='all')`, what is the specific condition required to remove a row?
- [ ] The row must have at least one missing value in any column
- [x] The row must be entirely composed of missing values across all columns
- [ ] The row must be a duplicate of another row in the DataFrame
- [ ] The row must have a missing value in the target variable column

### When dealing with 'unbalanced' numeric data (e.g., a feature that is 85% zero), why might we convert it to a binary '0 or 1' instead of leaving it as a continuous number?
- [ ] To make the model more complex and expressive
- [x] Because the presence or absence of the feature is likely more informative than the specific amount
- [ ] Because linear regression cannot process the number 0 in a feature column
- [ ] To ensure the mean and median of the feature become equal after transformation

### What is the conceptual logic behind using 'median replacement' for missing numeric data instead of 'mean replacement'?
- [ ] The median is always numerically larger than the mean for right-skewed data
- [x] The median is more robust to outliers and better represents the typical value in skewed data
- [ ] The mean cannot be computed mathematically when missing values are present
- [ ] The median is the standard imputation method only for categorical variables

### If we planned to use a more complex model (like a Neural Network) instead of Multiple Linear Regression, how might our data cleaning strategy change?
- [ ] We would probably remove even more features and rows from the dataset
- [x] We might retain more features and perform less aggressive simplification
- [ ] We would restrict the model to categorical variables only
- [ ] We would never remove outliers regardless of their influence on the model

### What is the risk of having too many 'one-hot encoded' columns in a linear model?
- [ ] The model will become too simple to capture any meaningful patterns
- [x] The model may suffer from the curse of dimensionality, making it harder to find a stable solution
- [ ] The model will automatically delete the target variable during training
- [ ] The R-squared value will be forced to a negative number

### Why is the `safe_drop` function used instead of just calling `df.drop()`?
- [ ] It runs significantly faster than the standard drop operation
- [x] It prevents the code from crashing if a column was already dropped in a prior cell
- [ ] It automatically saves a backup copy of the dropped data to disk
- [ ] It only operates on rows, never on columns

### The notebook mentions 'Structural Multicollinearity' when discussing yolked variables. Conceptually, what is multicollinearity?
- [ ] When the target variable follows a normal distribution
- [x] When independent features are highly correlated with each other
- [ ] When the model has more rows than it can process in memory
- [ ] When the residuals are random and uniformly distributed

### When the code uses `df.select_dtypes(include=np.number)`, what is it attempting to isolate?
- [ ] Only the single target variable column
- [x] Columns that contain continuous or discrete quantitative data
- [ ] Columns that store categorical string values
- [ ] Columns that have no missing values in any row

### Why is 'Year Sold' or 'Month Sold' sometimes considered an 'unbalanced' or 'uninformative' categorical variable in a housing dataset?
- [ ] Because people only buy houses during the summer months
- [x] Because if the distribution is nearly even and lacks a strong trend, it adds complexity without predictive gain
- [ ] Because years are numeric values and cannot be treated as categories
- [ ] Because the model already encodes the current year as a baseline feature

### What does the phrase 'Data cleaning is dictated by the model choice' mean in this context?
- [ ] All models require exactly the same cleaning steps regardless of their type
- [x] The assumptions of Multiple Linear Regression require specific data shapes like linearity and low noise
- [ ] The computer automatically performs all required cleaning once a model type is chosen
- [ ] Data should only be cleaned after the model has finished training

### When collapsing 'Foundation' into 'PConc', 'CBlock', and 'Other', why group the less frequent foundations into 'Other'?
- [ ] Because those foundation types are likely errors in the original data
- [x] To ensure the model has enough observations in each category to estimate a reliable coefficient
- [ ] To make the houses in the 'Other' category appear more expensive on average
- [ ] Because linear regression can only handle a maximum of three categories per feature

### What is the purpose of using `value_counts(normalize=True)` during the cleaning process?
- [ ] To see the raw integer count of every unique value in the column
- [x] To see the proportion of the data belonging to each category
- [ ] To compute the mean of a categorical variable numerically
- [ ] To identify and remove duplicate rows from the DataFrame

### If a feature is 90% 'Standard Circuit' and 10% other values, why would a modeler consider dropping it?
- [ ] Standard circuit breakers have no measurable impact on house sale prices
- [x] The lack of variation means the feature likely cannot differentiate expensive from cheap houses
- [ ] Because 10% is considered too high a proportion of missing data
- [ ] Because categorical features must be 100% consistent to be used in models

### In the context of the Ames dataset, what does 'Gr Liv Area' represent conceptually?
- [ ] Green Living Area — the eco-friendly or sustainable living space
- [x] Gross Living Area — the above-grade square footage of the home
- [ ] Ground Level Area — only the space on the first floor
- [ ] Garage and Living Area — the combined indoor and garage square footage

### Why is it important to look at the `.info()` output's 'Non-Null Count'?
- [ ] To determine the total number of feature columns in the dataset
- [x] To identify which columns have missing data that require imputation or removal
- [ ] To check the memory usage of the DataFrame in bytes
- [ ] To verify the data types of the target variable column

### The notebook describes dropping 'Misc Feature' and 'Misc Val.' What is the trade-off being made here?
- [ ] Trading predictive accuracy for faster training speed
- [x] Trading potential niche information for a simpler, less noisy model
- [ ] Trading categorical features for their equivalent numerical representations
- [ ] Trading training data for additional holdout test data

### Why is it standard practice to perform outlier removal *before* calculating medians for imputation?
- [x] Outliers can skew the median, so removing them first produces a more representative typical value
- [ ] The median cannot be calculated in the presence of any outlier values
- [ ] Removing outliers after imputation would increase the number of missing values
- [ ] The notebook author recommended this order based on personal preference

### What is the primary risk of 'over-cleaning' your data (dropping too many columns)?
- [ ] The model will become too complex and difficult to understand
- [x] You might remove a feature that has a strong, unique relationship with the target
- [ ] The computer will run out of available memory during training
- [ ] The dataset will become too large to process

### What does the code `df.loc[~df['Foundation'].isin(['PConc', 'CBlock']), 'Foundation'] = 'Other'` do?
- [ ] It deletes all rows where the foundation type is 'PConc' or 'CBlock'
- [x] It labels any foundation type that is NOT 'PConc' or 'CBlock' as 'Other'
- [ ] It creates a new DataFrame containing only rows with 'Other' foundations
- [ ] It checks whether the 'Foundation' column contains any numeric values

## 17_2_4_2: Forward/Backward Selection

### What is the primary purpose of 'One-Hot Encoding' in the context of the Ames dataset?
- [ ] To normalize numeric features to a standard scale
- [x] To convert categorical text into numeric 1s and 0s
- [ ] To handle outliers in the target variable
- [ ] To reduce the total number of features in the model

### Why is 'drop_first=True' critical when creating dummy variables for linear models?
- [x] It prevents the Dummy Variable Trap — perfect multicollinearity among encoded columns
- [ ] It makes the model run faster during the fitting process
- [ ] It automatically applies a log transformation to the encoded data
- [ ] It removes any rows with missing values from the encoded DataFrame

### In the provided code, why is a 'Pipeline' used during cross-validation?
- [ ] To speed up the OLS optimization algorithm
- [x] To prevent data leakage by ensuring scaling is only fit on training folds
- [ ] To allow categorical data to be used without any encoding step
- [ ] To automatically compute VIF scores for all features

### What does Scikit-Learn's 'SequentialFeatureSelector' do in 'forward' mode?
- [ ] Starts with all features and removes the worst one at each step
- [x] Starts with no features and adds the single best feature at each step
- [ ] Evaluates all possible feature subsets simultaneously
- [ ] Only considers features whose VIF score falls below a threshold of 5

### Why would you use only 2 or 3 cross-validation folds instead of 10 during model development?
- [ ] Using fewer folds is more accurate for large datasets
- [x] To reduce computation time during expensive sequential feature selection
- [ ] Because OLS cannot handle more than 5 cross-validation folds
- [ ] To ensure the R-squared value reported is higher than usual

### If 'Bedroom AbvGr' has a negative coefficient while 'Gr Liv Area' is held constant, what is the interpretation?
- [ ] Houses with more bedrooms are cheaper on average in all cases
- [x] More bedrooms in the same square footage means smaller, less desirable rooms
- [ ] The model is fundamentally broken and the coefficient cannot be trusted
- [ ] The number of bedrooms has no measurable impact on house price

### In a 'Residuals vs. Predicted Values' plot, what does a 'fan' or 'funnel' shape indicate?
- [ ] The model has achieved a perfect fit to the training data
- [x] Heteroscedasticity — non-constant error variance across predicted values
- [ ] The input features are highly correlated with each other
- [ ] The target variable follows a normal distribution

### What is the primary goal of log-transforming a right-skewed target variable?
- [ ] To make the regression coefficients numerically larger
- [ ] To handle non-linear relationships among the input features
- [x] To compress extreme values and make the distribution more symmetric
- [ ] To eliminate the need for an intercept term in the regression

### In the context of OLS, why can we use a squared term ($X^2$) and still call it a 'linear' model?
- [ ] Because the resulting graph of predictions is a straight line
- [x] Because the model is linear in its parameters (coefficients)
- [ ] Because $X^2$ evaluated at any single point is a straight line
- [ ] Because we apply a linear scaler to the squared term

### Adding a squared term ($X^2$) to a model allows the regression line to:
- [ ] Remain perfectly horizontal regardless of the predictor
- [x] Bend exactly once, forming a parabola shape
- [ ] Approximate a step-function boundary
- [ ] Ignore the influence of all outliers in the data

### What does a negative coefficient for a squared term (e.g., $-X^2$) typically represent in real estate data?
- [ ] Exponential growth in value as the feature increases
- [x] Diminishing returns — the curve flattens as X increases
- [ ] A perfectly linear relationship between the feature and price
- [ ] Data corruption or an error in the feature engineering step

### Why is it dangerous to use high-degree polynomials (e.g., $X^5$ or $X^{10}$)?
- [ ] The model becomes too simple to fit the training data
- [x] It leads to overfitting and a wiggly line that predicts poorly on new data
- [ ] OLS cannot solve polynomial equations with degree above 2
- [ ] The R-squared value will be forced to zero

### What is 'structural multicollinearity' in polynomial regression?
- [ ] Correlation caused by the inherent nature of the raw input data
- [x] Correlation between a base feature ($X$) and its derived square ($X^2$)
- [ ] Correlation between the target variable and the input features
- [ ] Missing values in categorical columns after encoding

### What is a common technique to reduce structural multicollinearity before squaring a feature?
- [ ] Log-transforming the feature before computing the square
- [x] Centering or standardizing the feature first
- [ ] Deleting the original linear version of the feature
- [ ] Removing the intercept term from the regression

### What does the Variance Inflation Factor (VIF) measure?
- [ ] The overall predictive accuracy of the regression model
- [x] How much a coefficient's variance is inflated due to correlation with other predictors
- [ ] The number of outliers detected in the training data
- [ ] The computation time required for the forward selection process

### Which VIF score is generally considered a 'red flag' indicating problematic multicollinearity?
- [ ] VIF = 0
- [ ] VIF = 1
- [x] VIF > 5 or 10
- [ ] VIF < 2

### If 'Overall Qual' and 'Overall Qual Squared' are both positive, the curve represents:
- [ ] Diminishing returns — price growth slows at higher quality
- [x] Accelerating growth — price increases faster at higher quality levels
- [ ] A flat horizontal line with no relationship to quality
- [ ] A downward-sloping trend in price

### In the Ames analysis, 'Gr Liv Area' had a VIF of ~118. What does this suggest?
- [ ] It is the only truly important predictor in the model
- [x] It is highly redundant with other square-footage variables in the dataset
- [ ] The data for this feature was collected with measurement error
- [ ] It is perfectly independent of all other features in the model

### What is the 'ideal' VIF score for a completely independent feature?
- [ ] 0
- [x] 1
- [ ] 10
- [ ] -1

### When interpreting a regression heatmap, a value of 1.0 between two independent variables suggests:
- [ ] Perfect independence between the two features
- [x] Perfect multicollinearity between the two features
- [ ] A perfectly negative relationship between the two features
- [ ] Both features follow a normal distribution

### If forward selection excludes a polynomial term you expected to be important, why might that happen?
- [ ] Forward selection does not support polynomial feature inputs
- [x] Other features already capture that polynomial's predictive power
- [ ] The R-squared was already at its maximum before the polynomial was considered
- [ ] Polynomial terms can only be evaluated using backward selection

### Why is 'StandardScaler' applied inside the Pipeline rather than to the whole dataset at once?
- [ ] To make the scaled data look more visually appealing in plots
- [x] To prevent data leakage from test and validation folds into the training process
- [ ] To ensure the target variable is also scaled alongside the features
- [ ] Because Scikit-Learn pipelines require a scaler as the first step

### A 'hockey stick' curve in house prices (upward bend) suggests that:
- [ ] Quality improvements have no measurable effect on sale price
- [x] High-end luxury finishes command an exponential price premium
- [ ] The house value is declining over time
- [ ] The model has high bias and is underfitting the data

### In the 'run_modeling_workflow' function, what is 'random_state=42' used for?
- [ ] To improve the model's R-squared score
- [x] To ensure the train-test split is reproducible across runs
- [ ] To randomly shuffle the column order before fitting
- [ ] To identify and flag outliers in the dataset

### If your model has high R-squared on training data but low R-squared on test data, it is:
- [ ] Underfitting the training data
- [x] Overfitting the training data
- [ ] Perfectly balanced between bias and variance
- [ ] Exhibiting high bias on both sets

### Which metric does the provided code use to evaluate the error of the model?
- [ ] R-squared
- [x] Mean Squared Error (MSE)
- [ ] Mean Absolute Error (MAE)
- [ ] VIF

### Backward selection starts with:
- [ ] No features in the model
- [x] All features in the model
- [ ] A random subset of features
- [ ] Only the categorical features

### Why is 'Gr Liv Area' often the strongest predictor in the Ames dataset?
- [ ] Because it is a log-transformed variable with low variance
- [x] Because square footage is a primary driver of home value
- [ ] Because it has the lowest VIF score among all features
- [ ] Because it was one-hot encoded before being passed to the model

### When a residual plot shows random scatter around the zero line, it suggests:
- [ ] The model is failing to capture structure in the data
- [x] The linear model assumptions are appropriate for the data
- [ ] There is high multicollinearity among the predictors
- [ ] The model is severely overfitting the training data

### What is the primary advantage of 'Forward Selection' over 'All Possible Subsets'?
- [ ] It is guaranteed to find the most accurate possible model
- [x] It is computationally much faster for large feature sets
- [ ] It automatically resolves multicollinearity issues
- [ ] It does not require a target variable to evaluate candidates

## 17_2_4_3: Regularization

### What is the primary objective of introducing regularization into a regression model?
- [ ] To find the absolute global minimum of the error function on training data
- [x] To penalize model complexity and discourage excessively large coefficients
- [ ] To increase the weight of the most important features to improve fit
- [ ] To eliminate the need for an intercept term in the regression equation

### In the context of standard OLS, why are 'massive weights' considered a problem?
- [ ] They make the model computationally too expensive to run on standard hardware
- [ ] They cause the model to ignore random noise in the training dataset
- [x] They make the model unstable — tiny input changes cause large prediction swings
- [ ] They produce an underfit model with high bias and low variance

### How does regularization 'compromise' during the training process?
- [x] It balances minimizing error with keeping feature weights small
- [ ] It balances training speed against the number of features selected
- [ ] It balances the mean squared error against the R-squared value
- [ ] It compromises between forward selection and backward selection strategies

### Compared to stepwise selection (Forward/Backward), regularization is described as 'nuanced' because:
- [ ] It can only remove features that have zero correlation with the target
- [ ] It is a binary process — each feature is either fully included or fully excluded
- [x] It can shrink coefficients to very small values without abruptly zeroing them out
- [ ] It requires fewer observations than features to produce valid results

### Why is stepwise selection considered 'unstable' compared to regularized models?
- [ ] Because it produces models with the highest possible mean squared error
- [x] Because small changes in training data can produce a completely different set of selected features
- [ ] Because it can only be applied to models with polynomial feature inputs
- [ ] Because it is mathematically incapable of producing a non-zero intercept

### Regarding computation, why did the Ames selection take 90 minutes with OLS but seconds with Regularization?
- [x] OLS selection requires training and comparing thousands of separate models sequentially
- [ ] Regularization uses a simplified version of the dataset with fewer rows
- [ ] OLS requires computing the logarithm of every feature value during selection
- [ ] Regularization skips the cross-validation step entirely during training

### What happens in Ridge Regression (L2) when Alpha is set to exactly 0?
- [ ] All coefficients are forced to exactly zero
- [ ] The model becomes equivalent to a LASSO regression
- [x] The model acts exactly like a standard OLS linear regression
- [ ] The regularization penalty becomes infinitely large

### Which statement best describes the coefficient shrinkage behavior of Ridge Regression?
- [ ] It forces unimportant feature coefficients to be exactly 0.0
- [x] It shrinks coefficients asymptotically toward zero but never forces them to exactly 0.0
- [ ] It only shrinks the intercept term while leaving all slope coefficients untouched
- [ ] It increases the coefficients of features that are highly correlated with each other

### Why is Ridge Regression (L2) particularly effective at handling multicollinearity?
- [ ] It picks the feature with the highest p-value and drops all the rest
- [ ] It merges correlated features into a single new composite feature
- [x] It distributes credit among correlated variables by shrinking their weights together
- [ ] It transforms all correlated variables into integer category codes

### The 'L2' in L2 regularization refers to which mathematical concept?
- [ ] Taking the square root of the total number of features
- [x] The L2 norm, which computes the penalty using squared coefficient values
- [ ] Having exactly two target variables in the regression model
- [ ] The second derivative of the loss function with respect to the coefficients

### If you have a domain where every variable is conceptually important and you don't want to lose any, which method is best?
- [ ] LASSO (L1)
- [ ] Backward Selection
- [x] Ridge (L2)
- [ ] Forward Selection

### What is a major limitation of Ridge Regression in terms of model 'decluttering'?
- [x] It cannot perform automatic feature selection or removal
- [ ] It requires the target variable to follow a normal distribution
- [ ] It is only compatible with categorical feature inputs
- [ ] It substantially increases the variance of the fitted model

### Why is 'Feature Scaling' (like StandardScaler) mandatory for both Ridge and Lasso?
- [x] The penalty is applied to coefficient size; unscaled features with large units get unfairly penalized
- [ ] The algorithms cannot process negative numbers in raw feature values
- [ ] Scaling ensures that the R-squared value remains between 0 and 1
- [ ] Regularization only converges correctly on normally distributed feature data

### How does regularization affect the 'Interpretability' of the resulting slopes (coefficients)?
- [ ] It makes them more accurate representations of real-world physical changes
- [x] It introduces mathematical bias, so slopes can no longer be interpreted as literal unit increases
- [ ] It makes the slopes equal to the Pearson correlation coefficient
- [ ] It has no effect on the interpretation of the regression slopes

### What does the acronym LASSO stand for?
- [ ] Linear Absolute Selection and Standard Operator
- [x] Least Absolute Shrinkage and Selection Operator
- [ ] Logarithmic Aligned Squared Standard Optimization
- [ ] Least Adaptive Selection and Shrinkage Optimizer

### What is the key mathematical difference between Lasso and Ridge penalties?
- [ ] Lasso uses squared values (L2); Ridge uses absolute values (L1)
- [x] Lasso uses absolute values (L1); Ridge uses squared values (L2)
- [ ] Lasso penalizes the intercept term; Ridge does not penalize the intercept
- [ ] Lasso operates on a log scale; Ridge operates on a linear scale

### Lasso is often preferred over Ridge when the goal is:
- [ ] To ensure no variable coefficient ever reaches exactly 0.0
- [ ] To keep the most complex and fully-specified model possible
- [x] To achieve automatic feature selection and produce a sparser model
- [ ] To eliminate the need for cross-validation during hyperparameter tuning

### If Lasso encounters two highly correlated features, how does it typically behave?
- [ ] It retains both features and assigns them equal weight
- [x] It tends to arbitrarily pick one to keep and drives the other to zero
- [ ] It averages the two features together into a single composite predictor
- [ ] It drops both correlated features to fully avoid multicollinearity

### In the Bias-Variance tradeoff, regularization intentionally:
- [ ] Increases variance to achieve a reduction in bias
- [x] Trades a small amount of training accuracy (bias) for better generalization (reduced variance)
- [ ] Eliminates both bias and variance simultaneously
- [ ] Decreases bias until training error reaches exactly zero

### What is ElasticNet Regression?
- [ ] A method that only operates on categorical feature data
- [ ] A regression that stretches data points to conform to a curved shape
- [x] A mathematical combination of both Lasso (L1) and Ridge (L2) penalties
- [ ] A regression method that requires no feature scaling before fitting

### In ElasticNet, what does an 'l1_ratio' of 1.0 signify?
- [ ] The model is 100% Ridge regression
- [ ] The model blends Lasso and Ridge at a 50/50 ratio
- [x] The model is 100% Lasso regression
- [ ] The model has no regularization penalty applied

### Why is ElasticNet useful in fields like genomics where there are 'more columns than rows' (p > n)?
- [x] It can handle high-dimensional settings where standard OLS and Lasso break down
- [ ] It automatically generates synthetic rows using resampling methods
- [ ] It requires no hyperparameter tuning in high-dimensional settings
- [ ] It is the only model that supports negative R-squared values

### What is the primary computational 'cost' of using ElasticNet over Ridge or Lasso?
- [ ] It requires double the memory to store the trained model on disk
- [x] It requires tuning two parameters (Alpha and l1_ratio) simultaneously, often needing a 2D grid search
- [ ] It can only be run on small datasets with fewer than 100 rows
- [ ] It makes residual diagnostic plots impossible to interpret

### According to the text, if a researcher only cares about 'predictive accuracy' and not interpretability, they usually move on to:
- [ ] Simple Linear Regression with a single feature
- [x] Tree-based models like Random Forests or XGBoost
- [ ] Manual feature selection using domain knowledge
- [ ] Ridge Regression with Alpha set to 0

### Regularization helps prevent a model from 'memorizing' random noise. This 'memorization' is also known as:
- [ ] Underfitting the training data
- [x] Overfitting the training data
- [ ] Global optimization of the loss function
- [ ] Standardization of the feature space

### In the Ames modeling workflow, why was 'SalePrice' log-transformed (y = np.log)?
- [ ] To make the calculation of the alpha hyperparameter easier
- [x] To handle right-skewness in the target variable distribution
- [ ] Because Lasso regression only operates on log-scaled target data
- [ ] To increase the total number of features available to the model

### Lasso Regression uses an L1 penalty. The '1' in L1 refers to:
- [ ] The fact that it selects only one feature per model run
- [x] Taking the absolute value to the power of 1
- [ ] Having only one alpha hyperparameter to tune
- [ ] Being the top-ranked method among Ridge regression enthusiasts

### If Alpha is set to an 'overwhelmingly high' value in Lasso, what is the likely result?
- [ ] Every feature will receive a very large positive coefficient
- [ ] The model will behave identically to a standard OLS regression
- [x] The model will drop all predictors and reduce to just the intercept
- [ ] The R-squared value will increase to its maximum possible value

### When comparing Ridge and ElasticNet behavior on highly correlated groups of variables:
- [ ] Ridge zeros out the whole group; ElasticNet retains one representative
- [x] Ridge keeps the whole group; ElasticNet can keep groups but also zeros out truly useless variables
- [ ] Both models behave identically to forward stepwise selection
- [ ] Neither model can handle correlated variables in the feature set

### Which step is 'CRITICAL' for linear models when performing One-Hot Encoding to prevent multicollinearity?
- [ ] Scaling the target variable before encoding
- [x] Setting 'drop_first=True'
- [ ] Removing all numeric columns before encoding
- [ ] Using cross-validation with 100 folds

## 17_2_4_4: Hyperparameter Tuning

### In the manual alpha tuning loop, you plot both training R² and cross-validation R² against alpha. As alpha increases from 0.01 to 1000, what pattern do you expect to see in the two curves?
- [ ] Training R² rises steadily while CV R² falls steadily, reflecting increasing model complexity
- [x] Training R² falls steadily while CV R² rises then falls, peaking at the optimal alpha
- [ ] Both curves rise in parallel, since regularization improves both training and test fit
- [ ] Training R² stays flat while CV R² oscillates randomly around a constant value

### A colleague runs GridSearchCV with 5-fold CV on a training set of 2,000 samples, testing 10 alpha values. How many total model fits does GridSearchCV perform?
- [ ] 10, because it fits one model per alpha value
- [x] 50, because it fits 10 alphas across 5 folds
- [ ] 2,000, because it fits one model per training sample
- [ ] 15, because it adds the number of alphas and folds together

### You use GridSearchCV with refit=True (the default). After the search completes, what model does grid_search.best_estimator_ return?
- [ ] The model trained on the single best-performing fold during the grid search
- [x] The model retrained on the entire training set using the best hyperparameters found
- [ ] An ensemble of all models tested during the search, weighted by CV score
- [ ] The model from the fold with the lowest validation error, without any retraining

### Why does the Pipeline prefix hyperparameter keys with 'model__' (e.g., 'model__alpha') when passing them to GridSearchCV?
- [ ] It is a naming convention that GridSearchCV ignores; any prefix would work
- [x] It tells GridSearchCV which step in the pipeline the parameter belongs to
- [ ] It prevents GridSearchCV from tuning the scaler's parameters simultaneously
- [ ] It signals to scikit-learn that the parameter should be tuned on a logarithmic scale

### You fit a StandardScaler on the full dataset, then pass the scaled data to cross_val_score with cv=5. What is the consequence?
- [ ] Nothing changes; scaling before CV is mathematically equivalent to scaling within each fold
- [x] The scaler's mean and variance incorporate information from every fold, leaking into each training split
- [ ] Cross-validation detects the pre-scaled data and adjusts its fold boundaries accordingly
- [ ] The model trains faster because the scaling computation runs only once instead of five times

### In ElasticNet, the l1_ratio parameter controls the mix between L1 and L2 penalties. What does l1_ratio=0.7 mean?
- [x] 70% of the penalty comes from the L1 (Lasso) term and 30% from the L2 (Ridge) term
- [ ] 70% of the features will be dropped to exactly zero by the fitted model
- [ ] The model behaves 70% like Ridge regression and 30% like Lasso regression
- [ ] The overall penalty strength is reduced to 70% of the specified alpha value

### After tuning three regularized models (Ridge, Lasso, ElasticNet) with GridSearchCV, all three achieve nearly identical test R² scores. What is the most likely explanation?
- [ ] The hyperparameter grids were too narrow to distinguish between the three models
- [x] The remaining features are weakly informative, so all regularization approaches converge to similar solutions
- [ ] GridSearchCV returns the same model regardless of the algorithm specified
- [ ] The test set is too small to distinguish between different modeling approaches

### You run GridSearchCV on a training set and find the best alpha. You then evaluate that model on a held-out test set and get R² = 0.83. Why might this score be slightly inflated?
- [ ] The test set is systematically easier to predict than the training set due to random sampling
- [x] The best alpha was selected because it happened to perform well on the specific CV folds, partially capturing noise
- [ ] GridSearchCV uses a different scoring metric than the one used for the final test evaluation
- [ ] The model was retrained on the full training set, which artificially boosts test performance

### In nested cross-validation with 5 outer folds and 5 inner folds, how many total model fits are performed when searching over 8 alpha values?
- [ ] 40, because there are 5 outer folds times 8 alpha values
- [x] 200, because each outer fold runs the full inner GridSearch of 5 folds × 8 alphas
- [ ] 25, because there are 5 outer folds times 5 inner folds
- [ ] 13, because you add the 5 outer folds, 5 inner folds, and 3 unique alpha values together

### In the nested CV code, cross_val_score is called with a GridSearchCV object as its estimator. What happens during each outer fold?
- [ ] cross_val_score extracts the best hyperparameters and fits a simple model with them
- [x] cross_val_score calls .fit() on the GridSearchCV, which runs the inner search, then predicts on the outer test fold
- [ ] cross_val_score skips the inner search and uses the base estimator's default hyperparameters
- [ ] cross_val_score averages the predictions across all hyperparameter combinations in the inner search

### Your nested CV results show Ridge R² = 0.7910 ± 0.0266, while the single train/test split gave R² = 0.8300. What does the ± 0.0266 tell you?
- [x] The model's performance varies by about 2.7 percentage points depending on the fold composition
- [ ] The nested CV code has a bug that introduces random noise into each score
- [ ] The inner loop's hyperparameter search is unstable and needs more alpha candidates
- [ ] The outer test folds contain outlier houses that distort the average score

### Why does nested CV use the full dataset (X, y) rather than just X_train, y_train?
- [x] Because nested CV replaces the train/test split — every point serves as both training and test data
- [ ] Because the outer loop requires more data than the inner loop for stable hyperparameter estimates
- [ ] Because GridSearchCV requires the full dataset to properly scale features before splitting
- [ ] Because using only the training set causes the inner loop to overfit more severely

### In nested CV, Outer Fold 1 selects alpha=1 as optimal, while Outer Fold 3 selects alpha=10. What does this tell you?
- [ ] The inner loop's random state was not fixed, causing non-reproducible results
- [x] The optimal regularization strength depends on which specific samples are in the training subset
- [ ] The model is fundamentally unstable and should not be deployed for any predictions
- [ ] One of the outer folds contains corrupted data that skews the hyperparameter search

### A learning curve shows training R² = 0.95 and validation R² = 0.70 at the largest training size, with a persistent gap. What should you do?
- [ ] Add more training data, since the validation curve may still be improving
- [ ] Increase model complexity, since both scores appear too low
- [x] Increase regularization or reduce model complexity, since the model is overfitting
- [ ] Switch to a different scoring metric, since R² is not appropriate for this dataset

### A learning curve shows both training and validation R² converging at approximately 0.55, with the validation curve flatlining as training size increases. What does this indicate?
- [ ] The model is overfitting and would benefit from more training data
- [x] The model is underfitting; more data will not help, and a more complex model is needed
- [ ] The model is well-calibrated and performing at its theoretical maximum
- [ ] The cross-validation folds are too small to produce reliable validation scores

### In the learning curve code, train_sizes=np.linspace(0.1, 1.0, 5) is used. How many different training set sizes are evaluated?
- [x] 5, representing 10%, 32.5%, 55%, 77.5%, and 100% of the training data
- [ ] 10, because linspace generates values from 0.1 to 1.0 in steps of 0.1
- [ ] 1, because the model is only trained on the full dataset at the final step
- [ ] 3, because the first and last values are excluded from evaluation

### You compare nested CV R² scores for Ridge (0.7910), Lasso (0.7910), and ElasticNet (0.7909). All three are nearly identical. What does this suggest about the underlying data?
- [x] The feature set is small enough that L1 and L2 penalties produce nearly equivalent shrinkage
- [ ] The GridSearchCV grids for all three models were identical, forcing the same solution
- [ ] The nested CV implementation has a bug that returns the same score for all models
- [ ] The data contains no predictive signal, so all models converge to the same baseline

### Which statement best describes the relationship between the inner loop's best CV score and the outer loop's average score in nested CV?
- [x] The inner loop's score is typically higher because it averages over smaller training subsets
- [ ] The outer loop's score is typically higher because it evaluates on larger test folds
- [ ] The two scores are always identical because they use the same scoring metric
- [ ] The inner loop's score is always lower because it uses more folds than the outer loop

### You are building a production model for a client who needs a trustworthy performance estimate. Your dataset has 1,500 samples. Which approach should you use?
- [ ] A single 80/20 train/test split with GridSearchCV, because it is fast and simple
- [x] Nested cross-validation, because it provides an unbiased estimate with quantifiable variance
- [ ] Leave-one-out cross-validation without hyperparameter tuning, because it uses all data for training
- [ ] A single model fit on all 1,500 samples with no validation, because the dataset is small

### In the bias-variance tradeoff plot for Ridge regression, the CV R² peaks at alpha=1. If you were to add alpha=0.5 and alpha=2 to your grid, what would you expect?
- [ ] Alpha=0.5 would have higher CV R² because lower alpha improves generalization
- [ ] Alpha=2 would have higher CV R² because higher alpha improves generalization
- [ ] Both would have lower CV R² than alpha=1, since alpha=1 is already at the peak
- [x] One would be higher and one lower, but you cannot predict which without running the experiment

### A student claims: 'Nested CV is unnecessary if I have a separate test set that I never touch during tuning.' What is the strongest counterargument?
- [ ] The student is correct; a separate test set completely eliminates the need for nested CV
- [x] A single test set gives only one performance estimate with no measure of its variability
- [ ] Nested CV is faster than a train/test split, so there is no reason not to use it
- [ ] The test set score is always lower than the nested CV score, making nested CV more conservative

### When using learning_curve() with cv=5, how is the validation score computed at each training size?
- [ ] The model trains once on the specified fraction and scores on the remaining data
- [x] For each training size, 5 models train on different folds and validation scores are averaged
- [ ] The model trains on the full dataset and scores on 5 different random subsets of the test set
- [ ] Training and validation scores are computed on the same data to measure overfitting

### You notice that the learning curve's validation score is still climbing at the largest training size. What is the most actionable next step?
- [ ] Reduce model complexity, since the model is overfitting at all training sizes
- [x] Collect more training data, since the model has not plateaued and can still improve
- [ ] Switch to a simpler model, since the current model is too complex for the available data
- [ ] Increase the number of cross-validation folds to get a more accurate validation score

### In the notebook, the 'Features Kept' column counts coefficients with absolute value > 1e-5. Why is this threshold used instead of checking for exact zeros?
- [x] Because Ridge regression never produces exact zeros; coefficients approach but never reach zero
- [ ] Because floating-point arithmetic makes exact zero comparisons unreliable for all models
- [ ] Because Lasso always produces coefficients slightly above zero due to numerical imprecision
- [ ] Because 1e-5 is the default regularization strength in scikit-learn

### You run nested CV with 5 outer folds and 3 inner folds, searching 12 hyperparameter combinations. Your manager asks how long it will take compared to a single GridSearchCV run (5 folds, 12 combinations). What do you tell them?
- [ ] About the same time, since the inner loop reuses computations from the outer loop
- [x] About 5 times longer, because the outer loop runs the full inner GridSearch once per outer fold
- [ ] About 15 times longer, because you multiply the outer folds, inner folds, and combinations
- [ ] Less time, because nested CV uses smaller training sets in each fold

### The notebook states that nested CV scores are 'typically lower' than train/test split scores. Under what circumstance might nested CV produce a higher score?
- [x] When the single test split happened to contain unusually difficult-to-predict samples by chance
- [ ] When the inner loop uses more folds than the outer loop, inflating the score
- [ ] When the model is severely underfitting, making all evaluation methods produce the same score
- [ ] This can never happen; nested CV always produces a lower score by definition

### In the GridSearchCV output, 'Fitting 5 folds for each of 20 candidates, totalling 100 fits' appears for ElasticNet. What are the 20 candidates?
- [ ] 20 different random splits of the training data into train and validation sets
- [x] The Cartesian product of 4 alpha values and 5 l1_ratio values, giving 20 hyperparameter combinations
- [ ] 20 different model architectures, each with a different number of features
- [ ] 20 iterations of the ElasticNet optimization algorithm before convergence

### You fit a learning curve and observe that training score starts at 0.99 with 10% of data and drops to 0.79 at 100%, while validation score rises from 0.50 to 0.78. What is happening?
- [x] The model overfits on small training sets and gradually improves generalization as more data is added
- [ ] The model underfits at all training sizes and needs a more complex architecture
- [ ] The CV folds are too small at low training sizes, producing unreliable scores
- [ ] The learning rate is too high, causing the model to diverge as more data is introduced

### A colleague argues that nested CV is 'wasteful' because it trains hundreds of models but only reports a single average score. How would you respond?
- [ ] They are correct; a single train/test split with GridSearchCV is sufficient for reporting
- [x] The standard deviation across outer folds is equally valuable — it quantifies model stability
- [ ] Nested CV produces a separate final model for each outer fold to deploy in production
- [ ] The computational cost of nested CV is negligible on modern hardware, so the objection is moot

### After running nested CV, you want to deploy a final model for production predictions. Which model should you deploy?
- [ ] The average of all models produced across the outer folds, to reduce variance
- [ ] The model from the outer fold with the highest test score, since it performed best
- [x] A fresh model trained on all data using the most common hyperparameters from the inner loops
- [ ] The model from the inner loop with the lowest validation error

## 17_2_4_5: Tree-Based Methods

### Linear models required manual polynomial features to capture non-linear patterns. How do tree-based models handle non-linearity?
- [ ] They apply logarithmic transformations to all features automatically
- [x] They partition the data into regions and predict the average within each region
- [ ] They add squared terms to the regression equation during training
- [ ] They use gradient descent to discover the optimal non-linear curve

### In a regression tree, what value does each leaf node predict?
- [ ] The median of target values of training samples in that leaf
- [x] The average of target values of training samples in that leaf
- [ ] The most frequent target value among training samples in that leaf
- [ ] A weighted sum of coefficients applied to the features in that leaf

### How does a regression tree decide which feature and split point to use at each node?
- [ ] It randomly selects a feature and tries all possible split points on that feature
- [ ] It selects the feature with the highest Pearson correlation to the target variable
- [x] It tries every possible split on every feature and picks the one that reduces error most
- [ ] It uses the feature that was most important in the parent node's split

### An unconstrained decision tree achieves R² = 1.0 on training data but R² = 0.80 on test data. What is happening?
- [ ] The model is underfitting because it has too few splits to capture the patterns
- [x] The model is overfitting by creating a separate leaf for nearly every training sample
- [ ] The training data contains outliers that the model is correctly ignoring
- [ ] The test set is fundamentally different due to data leakage during the split

### In the depth experiment, the test R² peaked at depth 7 and then declined as depth increased. What does this pattern represent?
- [x] The bias-variance tradeoff — deeper trees reduce bias but increase variance
- [ ] A computational memory limitation where deeper trees exhaust available RAM
- [ ] Learning rate decay, where deeper trees contribute less to the overall prediction
- [ ] A data quality issue where deeper splits encounter progressively noisier samples

### Why does a Random Forest use bootstrap sampling (drawing with replacement) for each tree?
- [ ] To ensure every tree sees the exact same data in a different random order
- [ ] To reduce the computational cost of training each individual tree
- [x] To give each tree a different perspective on the data so they make different errors
- [ ] To eliminate the need for a separate test set during model evaluation

### In a Random Forest, what does max_features='sqrt' mean at each split?
- [ ] Each tree considers only the square root of the total features across the entire tree
- [x] Each tree considers a random subset of about √n features at each individual node split
- [ ] The forest builds exactly √n trees, where n is the total number of features
- [ ] Each tree uses the top √n most important features and ignores the rest

### Why does feature randomness in Random Forests prevent every tree from splitting on the same dominant feature first?
- [ ] Because the dominant feature is removed from the dataset before training begins
- [x] Because each tree is forced to consider only a random subset of features at each node
- [ ] Because the dominant feature's values are randomly shuffled before each split
- [ ] Because the algorithm penalizes trees that use the same feature twice

### The Random Forest selected max_depth=None as optimal. Why is unlimited depth acceptable in a forest but catastrophic for a single tree?
- [ ] Because the forest uses a different splitting criterion that prevents overfitting
- [x] Because averaging across diverse trees cancels out individual tree overfitting
- [ ] Because each tree sees fewer features, making deep trees simpler to learn
- [ ] Because the forest automatically prunes trees that show signs of overfitting

### What is the primary objective of a Random Forest from a bias-variance perspective?
- [ ] Reducing bias by building progressively deeper trees sequentially
- [x] Reducing variance by averaging many diverse, uncorrelated predictions
- [ ] Reducing both bias and variance equally through random feature subsampling
- [ ] Eliminating bias entirely by using an infinite number of trees

### In gradient boosting, what does each new tree predict?
- [ ] The original target variable (e.g., house price) directly
- [x] The residuals (errors) left by the ensemble of all previous trees
- [ ] The average of all previous trees' predictions
- [ ] A weighted combination of the target and the prior ensemble's residuals

### Gradient boosting starts with an initial prediction equal to the average target value. What is the role of the first tree?
- [ ] It predicts the target variable directly, ignoring the initial constant prediction
- [x] It predicts the residuals from the initial prediction, correcting the average
- [ ] It replaces the initial prediction entirely with its own output
- [ ] It determines which features are most important for all subsequent trees

### What is the key difference between Random Forests and Gradient Boosting in how they build their ensembles?
- [ ] Random Forests build trees sequentially; Gradient Boosting builds them in parallel
- [x] Random Forests average independent trees; Gradient Boosting corrects errors sequentially
- [ ] Random Forests use deep trees; Gradient Boosting uses only single-split stumps
- [ ] Random Forests reduce bias; Gradient Boosting reduces variance

### Why does HistGradientBoostingRegressor use histogram binning?
- [ ] To convert categorical features into numeric codes automatically
- [x] To group continuous data into discrete bins, speeding up the split-finding process
- [ ] To reduce the number of features by merging correlated ones into bins
- [ ] To place missing values into a dedicated bin separate from observed data

### The raw-data HGB model achieved R² = 0.9449, nearly identical to the cleaned-data version. What does this demonstrate?
- [ ] That data cleaning is never necessary when using any tree-based model
- [x] That tree-based models are robust to messy data and need less preprocessing
- [ ] That the Ames dataset was already clean enough that no preprocessing was needed
- [ ] That HGB automatically replicates the cleaning steps performed in the notebook

### How does XGBoost's built-in regularization differ from Ridge and Lasso regularization?
- [x] XGBoost applies penalties to leaf weights and tree complexity, not to linear coefficients
- [ ] XGBoost only uses L1 regularization, while Ridge and Lasso both use L1 and L2
- [ ] XGBoost applies regularization after training, while Ridge and Lasso apply it during training
- [ ] XGBoost relies solely on tree depth limits and has no explicit regularization

### Boosting is inherently sequential, making it hard to parallelize. How does XGBoost achieve speed improvements?
- [ ] It builds multiple trees in parallel and averages their predictions
- [x] It parallelizes the internal work of building each tree, such as feature sorting
- [ ] It skips the residual calculation for trees after the first 50
- [ ] It uses a single CPU core but with a more efficient optimization algorithm

### How does XGBoost handle missing values at each split?
- [ ] It imputes missing values with the median before training begins
- [ ] It removes any sample with a missing value from the training set
- [x] It tests sending missing values left or right and learns which direction reduces error
- [ ] It assigns missing values to a separate leaf that predicts the global average

### Feature importance in tree models measures how much each split reduced prediction error. What is a key limitation of this metric?
- [ ] It cannot handle categorical features, so their importance is always reported as zero
- [x] It tells you what the model prioritizes but not how the feature affects the target
- [ ] It only applies to the top 5 features and ignores all remaining predictors
- [ ] It requires the model to be retrained after importance scores are computed

### In the feature importance plot, Overall Qual accounts for 45.76% of the model's learning. What does this percentage mean?
- [ ] Overall Qual alone can predict 45.76% of house prices without any other features
- [x] Splits on Overall Qual were responsible for 45.76% of total error reduction across all trees
- [ ] 45.76% of the trees in the ensemble used Overall Qual as their first split
- [ ] Overall Qual has a coefficient of 0.4576 in the underlying linear approximation

### Gr Liv Area has a VIF of 118 (extreme multicollinearity) but only 2.98% feature importance. Why?
- [ ] Living area is genuinely unimportant for predicting house prices in Ames
- [x] XGBoost chose correlated features like Overall Qual for splits, leaving Gr Liv Area with little credit
- [ ] The VIF calculation is only relevant for linear models and is meaningless for trees
- [ ] The feature importance calculation is biased against continuous numeric features

### In the model comparison, XGBoost's single-split score was 0.9418 but its nested CV estimate was 0.9177. What explains this gap?
- [ ] The nested CV used a different scoring metric that is inherently more conservative
- [x] The single-split score was inflated because the same data influenced both tuning and evaluation
- [ ] The nested CV used fewer trees, resulting in a weaker model
- [ ] The test set in the single split happened to contain easier-to-predict houses

### The nested CV standard deviation for XGBoost was ±0.0195. What does this tell you?
- [x] The model's performance varies by about 2 percentage points across different data splits
- [ ] The hyperparameter search found 19.5% of the possible combinations to be optimal
- [ ] The model is overfitting by approximately 1.95 percentage points on each fold
- [ ] The inner loop's best score exceeded the outer loop's score by 1.95 percentage points

### The notebook recommends starting with a Random Forest for most real-world problems. Why?
- [ ] Random Forests produce the highest accuracy of any model on structured data
- [x] Random Forests are fast, robust, and harder to misconfigure than boosting models
- [ ] Random Forests are the only tree method that handles missing values natively
- [ ] Random Forests provide the most interpretable feature importance scores

### What tradeoff do you accept when moving from linear regression to XGBoost?
- [ ] You gain interpretability but lose the ability to model non-linear patterns
- [x] You gain predictive accuracy but lose the ability to explain individual coefficient effects
- [ ] You gain speed but lose the ability to handle categorical features
- [ ] You gain robustness to outliers but lose the ability to use cross-validation

### In the nested CV for XGBoost, the inner loop used 3 folds and the outer loop used 5 folds with 12 hyperparameter combinations. How many total model fits occurred?
- [ ] 60, because 5 outer folds times 12 hyperparameter combinations equals 60
- [x] 180, because 5 outer folds each run a full inner search of 12 combinations × 3 inner folds
- [ ] 15, because 5 outer folds times 3 inner folds equals 15 total evaluations
- [ ] 36, because 12 hyperparameter combinations times 3 inner folds equals 36

### Central Air achieved 7.86% feature importance despite being a binary feature. Why might a binary feature score highly?
- [ ] Binary features are weighted more heavily than continuous features in tree models
- [x] The presence or absence of central air is a major price differentiator in the Ames market
- [ ] Binary features are used more often as split points because they are computationally simpler
- [ ] The model automatically upweights binary features to compensate for their limited range

### The notebook mentions that partial dependence plots:
- [ ] Identify which features should be removed from the model entirely
- [x] Reveal the direction and shape of a feature's effect on the prediction
- [ ] Calculate the exact dollar value contribution of each feature
- [ ] Determine the optimal hyperparameters for the model

### Decision trees convert categorical features to numeric codes using .cat.codes. What risk does this introduce?
- [x] The model treats categories as having an ordinal relationship that may not exist
- [ ] The model loses all information about the categorical feature during conversion
- [ ] The model becomes unable to handle new categories that appear in the test set
- [ ] The model requires one-hot encoding to work correctly with numeric codes

### The notebook's final model comparison shows Decision Tree (0.83) < Random Forest (0.88) < HGB (0.94) < XGBoost (0.94). What does this progression illustrate?
- [ ] That more complex models produce better results regardless of the dataset
- [x] That each ensemble method builds on the previous one's weaknesses to improve accuracy
- [ ] That the differences between models are negligible and any tree model would suffice
- [ ] That the hyperparameter grids were progressively larger, giving later models an advantage
