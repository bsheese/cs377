# 17.2 · Multiple Linear Regression

## 17_2_4_1: Data Cleaning

### In data cleaning, what is the primary conceptual reason for removing 'extreme outliers' (like very large houses sold for very low prices) before building a linear model?
- [ ] To increase the size of the dataset
- [x] To prevent a few unusual observations from disproportionately skewing the regression line
- [ ] To ensure the model can only predict luxury home prices
- [ ] Because linear regression cannot mathematically process large numbers

### Why is it conceptually important to remove unique identifiers like 'PID' or 'Order' from a machine learning featureset?
- [ ] They are too large to store in memory
- [ ] They are strings and linear regression requires numbers
- [x] They provide no generalizable information and could lead to 'memorizing' specific rows
- [ ] They are always perfectly correlated with the target variable

### What is the statistical problem with a 'monotonic' feature (a column where every single value is the same)?
- [x] It has zero variance and therefore provides no predictive information
- [ ] It will cause the model to crash due to 'division by zero'
- [ ] It makes the R-squared value too high to be realistic
- [ ] It is considered a 'yolked' variable

### The notebook describes 'yolked features' (e.g., Pool Quality and Pool Area). What is the primary concern when these two features are included in a model together?
- [x] They represent redundant information where one value is structurally dependent on the other
- [ ] They are always missing from the dataset at the same time
- [ ] They are measured in different units
- [ ] They cause the target variable to become right-skewed

### If a house has 'None' for a basement category and '0' for basement square footage, what does this tell us about the relationship between these features?
- [ ] The data is corrupted
- [x] The categorical 'None' is a structural prerequisite for the numerical '0'
- [ ] The two variables are independent
- [ ] One is an outlier of the other

### Why would a data scientist choose to drop a feature like 'Pool Area' if 99% of the entries are zero?
- [ ] Because zeros cannot be used in a linear regression
- [x] To reduce 'noise' and complexity in the model for a very small gain in predictive power
- [ ] Because the model only works for houses with pools
- [ ] To ensure the dataset is perfectly balanced

### In the context of 'Garage Yr Blt', why is it dangerous to simply replace missing values with '0'?
- [x] Because a year of '0' acts as a massive outlier that distorts the temporal logic of the model
- [ ] Because Python does not recognize 0 as a valid year
- [ ] Because it will make the house appear newer than it actually is
- [ ] Because 0 is reserved for categorical data only

### What is the conceptual goal of 'collapsing' a categorical variable with 10 categories into just 'Category A' and 'Other'?
- [ ] To increase the number of features in the model
- [x] To simplify the model and prevent 'sparsity' (too many columns with mostly zeros)
- [ ] To make the data more difficult to interpret
- [ ] To ensure the model is non-linear

### Why do we check for 'unbalanced' categoricals (e.g., where 95% of houses are on a 'Paved Street')?
- [ ] Because we want to find the most popular type of house
- [x] Because variables with very little variance provide almost no 'signal' for the model to learn from
- [ ] Because linear regression requires a 50/50 split in all categories
- [ ] To identify which houses are the most expensive

### Consider the code: `df['garage_attached'] = np.where(df['Garage Type'] == 'Attchd', 1, 0)`. What is the logical purpose of this transformation?
- [ ] To delete the 'Garage Type' column
- [x] To create a binary indicator (dummy variable) focusing on one specific attribute
- [ ] To calculate the average price of attached garages
- [ ] To normalize the garage area

### What is the primary benefit of converting binary string columns (like 'Yes'/'No') into the 'boolean' or 'bool' data type?
- [ ] It makes the file size larger
- [x] It clearly communicates to the model (and the programmer) that the data is a logical toggle
- [ ] It allows the model to handle missing values automatically
- [ ] It prevents the data from being used in a heatmap

### In the code `df.dropna(how='all')`, what is the specific condition required to remove a row?
- [ ] The row must have at least one missing value
- [x] The row must be entirely composed of missing values
- [ ] The row must be a duplicate of another row
- [ ] The row must have a missing Sale Price

### When dealing with 'unbalanced' numeric data (e.g., a feature that is 85% zero), why might we convert it to a binary '0 or 1' instead of leaving it as a continuous number?
- [ ] To make the model more complex
- [x] Because the presence/absence of the feature is likely more informative than the specific amount
- [ ] Because linear regression cannot handle the number 0
- [ ] To ensure the mean and median are the same

### What is the conceptual logic behind using 'median replacement' for missing numeric data instead of 'mean replacement'?
- [ ] The median is always higher than the mean
- [x] The median is more robust to outliers and better represents the 'typical' value in skewed data
- [ ] The mean is mathematically impossible to calculate with missing data
- [ ] The median is only used for categorical data

### If we planned to use a more complex model (like a Neural Network) instead of Multiple Linear Regression, how might our data cleaning strategy change?
- [ ] We would probably remove even more data
- [x] We might leave in more features and perform less aggressive 'simplification'
- [ ] We would only use categorical data
- [ ] We would never remove outliers

### What is the risk of having too many 'one-hot encoded' columns in a linear model?
- [ ] The model will become too simple
- [x] The model may suffer from the 'curse of dimensionality,' making it harder to find a stable solution
- [ ] The model will automatically delete the target variable
- [ ] The R-squared will always be negative

### Why is the `safe_drop` function used instead of just calling `df.drop()`?
- [ ] It makes the code run significantly faster
- [x] It prevents the code from crashing if you run the same cell twice or if a column is already gone
- [ ] It automatically saves a copy of the dropped data to the hard drive
- [ ] It only drops rows, never columns

### The notebook mentions 'Structural Multicollinearity' when discussing yolked variables. Conceptually, what is multicollinearity?
- [ ] When the target variable is normally distributed
- [x] When independent variables (features) are highly correlated with each other
- [ ] When the model has too many rows
- [ ] When the errors are random

### When the code uses `df.select_dtypes(include=np.number)`, what is it attempting to isolate?
- [ ] Only the target variable
- [x] Columns that contain continuous or discrete quantitative data
- [ ] Columns that are categorical in nature
- [ ] Columns that have no missing values

### Why is 'Year Sold' or 'Month Sold' sometimes considered an 'unbalanced' or 'uninformative' categorical variable in a housing dataset?
- [ ] Because people only buy houses in the summer
- [x] Because if the distribution is nearly even or lacks a strong trend, it adds complexity without predictive gain
- [ ] Because years are numbers and cannot be categories
- [ ] Because the model already knows the current year

### What does the phrase 'Data cleaning is dictated by the model choice' mean in this context?
- [ ] All models require the exact same cleaning steps
- [x] The assumptions and limitations of Multiple Linear Regression require specific data shapes (like linearity and lack of noise)
- [ ] The data is cleaned by the computer automatically once a model is chosen
- [ ] You should only clean data after the model is finished

### When collapsing 'Foundation' into 'PConc', 'CBlock', and 'Other', why group the less frequent foundations into 'Other'?
- [ ] Because they are probably errors in the data
- [x] To ensure the model has enough observations in each category to find a reliable coefficient
- [ ] To make the houses in the 'Other' category more expensive
- [ ] Because linear regression can only handle three categories maximum

### What is the purpose of using `value_counts(normalize=True)` during the cleaning process?
- [ ] To see the raw count of every value
- [x] To see the proportion (percentage) of the data belonging to each category
- [ ] To find the mean of a categorical variable
- [ ] To identify and remove duplicates

### If a feature is 90% 'Standard Circuit' and 10% other values, why would a modeler consider dropping it?
- [ ] Standard circuit breakers are not important for house prices
- [x] The lack of variation means the feature likely won't help the model differentiate between expensive and cheap houses
- [ ] Because 10% is too much missing data
- [ ] Because categorical data must be 100% consistent

### In the context of the Ames dataset, what does 'Gr Liv Area' represent conceptually?
- [ ] Green Living Area (eco-friendly space)
- [x] Gross Living Area (above-grade square footage)
- [ ] Ground Level Area only
- [ ] Garage and Living Area combined

### Why is it important to look at the `.info()` output's 'Non-Null Count'?
- [ ] To determine how many features are in the dataset
- [x] To identify which columns have missing data that require imputation or dropping
- [ ] To see the memory usage of the dataframe
- [ ] To check the data types of the target variable

### The notebook describes dropping 'Misc Feature' and 'Misc Val.' What is the trade-off being made here?
- [ ] Trading accuracy for speed
- [x] Trading potential niche information for a simpler, less 'noisy' model
- [ ] Trading categorical data for numerical data
- [ ] Trading training data for test data

### Why is it standard practice to perform outlier removal *before* calculating medians for imputation?
- [x] Outliers can skew the median (though less than the mean), so removing them first ensures a more representative 'typical' value
- [ ] The median cannot be calculated if outliers are present
- [ ] To increase the number of missing values
- [ ] Because the author recommended it

### What is the primary risk of 'over-cleaning' your data (dropping too many columns)?
- [ ] The model will become too complex to understand
- [x] You might remove a feature that actually has a strong, unique relationship with the target variable
- [ ] The computer will run out of memory
- [ ] The dataset will become too large

### What does the code `df.loc[~df['Foundation'].isin(['PConc', 'CBlock']), 'Foundation'] = 'Other'` do?
- [ ] It deletes all foundations that are 'PConc' or 'CBlock'
- [x] It labels any foundation type that is NOT 'PConc' or 'CBlock' as 'Other'
- [ ] It creates a new dataframe containing only 'Other' foundations
- [ ] It checks if 'Foundation' is a numeric column

## 17_2_4_2: Forward/Backward Selection

### What is the primary purpose of 'One-Hot Encoding' in the context of the Ames dataset?
- [ ] To normalize numeric features
- [x] To convert categorical text into numeric 1s and 0s
- [ ] To handle outliers in the target variable
- [ ] To reduce the number of features

### Why is 'drop_first=True' critical when creating dummy variables for linear models?
- [x] It prevents the 'Dummy Variable Trap' (perfect multicollinearity)
- [ ] It makes the model run faster
- [ ] It automatically log-transformed the data
- [ ] It removes rows with missing values

### In the provided code, why is a 'Pipeline' used during cross-validation?
- [ ] To speed up the OLS algorithm
- [x] To prevent data leakage by ensuring scaling is only fit on training folds
- [ ] To allow the use of categorical data without encoding
- [ ] To automatically calculate VIF scores

### What does Scikit-Learn's 'SequentialFeatureSelector' do in 'forward' mode?
- [ ] Removes the worst features one by one
- [x] Starts empty and adds the best feature at each step
- [ ] Checks all possible combinations of features simultaneously
- [ ] Only selects features with a VIF below 5

### Why would you use only 2 or 3 cross-validation folds instead of 10 during model development?
- [ ] It is more accurate for large datasets
- [x] To reduce computation time during expensive feature selection
- [ ] Because OLS cannot handle more than 5 folds
- [ ] To ensure the R-squared value is higher

### If 'Bedroom AbvGr' has a negative coefficient while 'Gr Liv Area' is held constant, what is the interpretation?
- [ ] Houses with more bedrooms are always cheaper
- [x] More bedrooms in the same square footage leads to smaller, less desirable rooms
- [ ] The model is fundamentally broken
- [ ] Bedrooms have no impact on house price

### In a 'Residuals vs. Predicted Values' plot, what does a 'fan' or 'funnel' shape indicate?
- [ ] The model is perfect
- [x] Heteroscedasticity (non-constant error variance)
- [ ] The features are highly correlated
- [ ] The target variable is normally distributed

### What is the primary goal of log-transforming a right-skewed target variable?
- [ ] To make the coefficients larger
- [ ] To handle non-linear features
- [x] To squash extreme values and make the distribution more 'normal'
- [ ] To eliminate the need for a constant intercept

### In the context of OLS, why can we use a squared term ($X^2$) and still call it a 'linear' model?
- [ ] Because the resulting graph is a straight line
- [x] Because the model is linear in its parameters (coefficients)
- [ ] Because $X^2$ is always a straight line
- [ ] Because we use a linear scaler

### Adding a squared term ($X^2$) to a model allows the regression line to:
- [ ] Stay perfectly horizontal
- [x] Bend exactly once (forming a parabola)
- [ ] Become a step-function
- [ ] Ignore all outliers

### What does a negative coefficient for a squared term (e.g., $-X^2$) typically represent in real estate data?
- [ ] Exponential growth
- [x] Diminishing returns (curve flattens out as $X$ increases)
- [ ] A perfectly linear relationship
- [ ] Data corruption

### Why is it dangerous to use high-degree polynomials (e.g., $X^5$ or $X^{10}$)?
- [ ] The model becomes too simple
- [x] It leads to overfitting and a 'wiggly' line that predicts poorly on new data
- [ ] OLS cannot solve equations above degree 2
- [ ] The R-squared will always be 0

### What is 'structural multicollinearity' in polynomial regression?
- [ ] Correlation caused by the inherent nature of the raw data
- [x] Correlation between a base feature ($X$) and its derived square ($X^2$)
- [ ] Correlation between the target and the features
- [ ] Missing data in the categorical columns

### What is a common technique to reduce structural multicollinearity before squaring a feature?
- [ ] Log-transforming the feature
- [x] Centering or standardizing the feature
- [ ] Deleting the original linear feature
- [ ] Removing the intercept

### What does the Variance Inflation Factor (VIF) measure?
- [ ] The accuracy of the model
- [x] How much the variance of a coefficient is 'inflated' due to correlation with other predictors
- [ ] The number of outliers in the data
- [ ] The speed of the forward selection process

### Which VIF score is generally considered a 'red flag' indicating problematic multicollinearity?
- [ ] VIF = 0
- [ ] VIF = 1
- [x] VIF > 5 or 10
- [ ] VIF < 2

### If 'Overall Qual' and 'Overall Qual Squared' are both positive, the curve represents:
- [ ] Diminishing returns
- [x] Accelerating/Exponential-like growth
- [ ] A straight horizontal line
- [ ] A downward trend

### In the Ames analysis, 'Gr Liv Area' had a VIF of ~118. What does this suggest?
- [ ] It is the only important variable
- [x] It is highly redundant with other square-footage variables
- [ ] The data was collected incorrectly
- [ ] It is perfectly independent of other variables

### What is the 'ideal' VIF score for a completely independent feature?
- [ ] 0
- [x] 1
- [ ] 10
- [ ] -1

### When interpreting a regression heatmap, a value of 1.0 between two independent variables suggests:
- [ ] Perfect independence
- [x] Perfect multicollinearity
- [ ] A negative relationship
- [ ] Normal distribution

### If forward selection excludes a polynomial term you expected to be important, why might that happen?
- [ ] Forward selection doesn't support math
- [x] Other features already capture that predictive power
- [ ] The R-squared is already too high
- [ ] Polynomials can only be used with backward selection

### Why is 'StandardScaler' applied inside the Pipeline rather than to the whole dataset at once?
- [ ] To make the data look better
- [x] To prevent data leakage from the test/validation folds into the training process
- [ ] To make sure the target variable is scaled
- [ ] Because Scikit-Learn requires it

### A 'hockey stick' curve in house prices (upward bend) suggests that:
- [ ] Quality increases don't matter
- [x] High-end luxury finishes command an exponential premium
- [ ] The house is losing value over time
- [ ] The model has high bias

### In the 'run_modeling_workflow' function, what is 'random_state=42' used for?
- [ ] To improve the R-squared
- [x] To ensure the train-test split is reproducible
- [ ] To shuffle the columns
- [ ] To identify outliers

### If your model has high R-squared on training data but low R-squared on test data, it is:
- [ ] Underfitting
- [x] Overfitting
- [ ] Perfectly Balanced
- [ ] Biased

### Which metric does the provided code use to evaluate the error of the model?
- [ ] R-squared
- [x] Mean Squared Error (MSE)
- [ ] Mean Absolute Error (MAE)
- [ ] VIF

### Backward selection starts with:
- [ ] No features
- [x] All features
- [ ] A random set of features
- [ ] Only categorical features

### Why is 'Gr Liv Area' often the strongest predictor in the Ames dataset?
- [ ] Because it's a log-transform
- [x] Because square footage is a primary driver of home value
- [ ] Because it has the lowest VIF
- [ ] Because it was one-hot encoded

### When a residual plot shows random scatter around the zero line, it suggests:
- [ ] The model is failing
- [x] The linear assumptions are appropriate
- [ ] There is high multicollinearity
- [ ] The model is overfitting

### What is the primary advantage of 'Forward Selection' over 'All Possible Subsets'?
- [ ] It is more accurate
- [x] It is computationally much faster
- [ ] It ignores multicollinearity
- [ ] It doesn't require a target variable

## 17_2_4_3: Regularization

### What is the primary objective of introducing regularization into a regression model?
- [ ] To find the absolute global minimum of the error function on training data.
- [x] To penalize model complexity and discourage excessively large coefficients.
- [ ] To increase the weight of the most important features to improve fit.
- [ ] To eliminate the need for an intercept in the regression equation.

### In the context of standard OLS, why are 'massive weights' considered a problem?
- [ ] They make the model computationally too expensive to run.
- [ ] They cause the model to ignore random noise in the dataset.
- [x] They make the model unstable, where tiny input changes cause massive prediction swings.
- [ ] They lead to an underfit model with high bias.

### How does regularization 'compromise' during the training process?
- [x] It balances minimizing error with keeping feature weights small.
- [ ] It balances training speed with the number of features used.
- [ ] It balances the mean squared error with the R-squared value.
- [ ] It compromises between forward and backward selection.

### Compared to stepwise selection (Forward/Backward), regularization is described as 'nuanced' because:
- [ ] It can only remove features that have zero correlation with the target.
- [ ] It is a binary process that decides if a feature is 100% in or out.
- [x] It can shrink coefficients to very small numbers without abruptly muting them.
- [ ] It requires fewer observations than features to work.

### Why is stepwise selection considered 'unstable' compared to regularized models?
- [ ] Because it always results in a model with the highest possible MSE.
- [x] Because small changes in training data can lead to a completely different set of chosen features.
- [ ] Because it can only be used with polynomial features.
- [ ] Because it is mathematically incapable of reaching an intercept.

### Regarding computation, why did the Ames selection take 90 minutes with OLS but seconds with Regularization?
- [x] OLS selection requires training and comparing thousands of separate models sequentially.
- [ ] Regularization uses a simpler version of the dataset with fewer rows.
- [ ] OLS requires the computer to calculate the log of every feature.
- [ ] Regularization skips the cross-validation step entirely.

### What happens in Ridge Regression (L2) when Alpha is set to exactly 0?
- [ ] All coefficients are forced to zero.
- [ ] The model becomes a LASSO regression.
- [x] The model acts exactly like a standard OLS linear regression.
- [ ] The penalty becomes infinitely large.

### Which statement best describes the coefficient shrinkage behavior of Ridge Regression?
- [ ] It forces unimportant features to be exactly 0.0.
- [x] It shrinks coefficients asymptotically toward zero but never forces them to exactly 0.0.
- [ ] It only shrinks the intercept while leaving slopes untouched.
- [ ] It increases coefficients for features that are highly correlated.

### Why is Ridge Regression (L2) particularly effective at handling multicollinearity?
- [ ] It picks the feature with the highest p-value and drops the rest.
- [ ] It combines correlated features into a single new feature.
- [x] It distributes 'credit' among correlated variables, shrinking their weights together.
- [ ] It transforms all correlated variables into categorical integers.

### The 'L2' in L2 regularization refers to which mathematical concept?
- [ ] Taking the square root of the number of features.
- [x] The L2 norm, which calculates the penalty based on the squared value of coefficients.
- [ ] Having exactly two target variables in the model.
- [ ] The second derivative of the loss function.

### If you have a domain where every variable is conceptually important and you don't want to lose any, which method is best?
- [ ] LASSO (L1)
- [ ] Backward Selection
- [x] Ridge (L2)
- [ ] Forward Selection

### What is a major limitation of Ridge Regression in terms of model 'decluttering'?
- [x] It cannot perform automatic feature selection/removal.
- [ ] It requires the target variable to be normally distributed.
- [ ] It is only compatible with categorical data.
- [ ] It significantly increases the variance of the model.

### Why is 'Feature Scaling' (like StandardScaler) mandatory for both Ridge and Lasso?
- [x] Because the penalty is applied to the coefficient size; unscaled data would unfairly penalize features with large units.
- [ ] Because the algorithm cannot handle negative numbers in the raw data.
- [ ] Because it ensures that the R-squared value remains between 0 and 1.
- [ ] Because regularization only works on data that is normally distributed.

### How does regularization affect the 'Interpretability' of the resulting slopes (coefficients)?
- [ ] It makes them more accurate representations of real-world physical changes.
- [x] It introduces mathematical bias, meaning slopes can no longer be interpreted as literal unit increases.
- [ ] It makes the slopes exactly equal to the correlation coefficient.
- [ ] It has no effect on the interpretation of the slopes.

### What does the acronym LASSO stand for?
- [ ] Linear Absolute Selection and Standard Operator
- [x] Least Absolute Shrinkage and Selection Operator
- [ ] Logarithmic Aligned Squared Standard Optimization
- [ ] Least Adaptive Selection and Shrinkage Optimizer

### What is the key mathematical difference between Lasso and Ridge penalties?
- [ ] Lasso uses squared values (L2); Ridge uses absolute values (L1).
- [x] Lasso uses absolute values (L1); Ridge uses squared values (L2).
- [ ] Lasso penalizes the intercept; Ridge does not.
- [ ] Lasso uses a log scale; Ridge uses a linear scale.

### Lasso is often preferred over Ridge when the goal is:
- [ ] To ensure no variable ever reaches 0.0.
- [ ] To keep the most complex model possible.
- [x] To achieve automatic feature selection and create a simpler model.
- [ ] To eliminate the need for cross-validation.

### If Lasso encounters two highly correlated features, how does it typically behave?
- [ ] It keeps both and gives them equal weights.
- [x] It tends to arbitrarily pick one to keep and drops the other to zero.
- [ ] It averages the two features together.
- [ ] It drops both features to avoid multicollinearity.

### In the Bias-Variance tradeoff, regularization intentionally:
- [ ] Increases variance to reduce bias.
- [x] Trades a bit of training accuracy (bias) for better generalization (reduced variance).
- [ ] Eliminates both bias and variance simultaneously.
- [ ] Decreases bias to ensure the training error is 0.

### What is ElasticNet Regression?
- [ ] A method that uses only categorical data.
- [ ] A regression that stretches the data points to fit a curve.
- [x] A mathematical combination of both Lasso (L1) and Ridge (L2) penalties.
- [ ] A type of regression that does not require feature scaling.

### In ElasticNet, what does an 'l1_ratio' of 1.0 signify?
- [ ] The model is 100% Ridge.
- [ ] The model is a 50/50 blend of Lasso and Ridge.
- [x] The model is 100% Lasso.
- [ ] The model has no regularization penalty.

### Why is ElasticNet useful in fields like genomics where there are 'more columns than rows' (p > n)?
- [x] It can handle datasets where standard OLS and Lasso mathematically break down.
- [ ] It automatically creates new rows of data using synthetic sampling.
- [ ] It requires no hyperparameter tuning.
- [ ] It is the only model that allows for negative R-squared values.

### What is the primary computational 'cost' of using ElasticNet over Ridge or Lasso?
- [ ] It requires double the memory on the hard drive.
- [x] It requires tuning two parameters (Alpha and L1_ratio) simultaneously, often needing a 2D Grid Search.
- [ ] It can only be run on small datasets with under 100 rows.
- [ ] It makes the residual plots impossible to read.

### According to the text, if a researcher only cares about 'predictive accuracy' and not interpretability, they usually move on to:
- [ ] Simple Linear Regression
- [x] Tree-based models (like Random Forests or XGBoost)
- [ ] Manual Feature Selection
- [ ] Ridge Regression with Alpha=0

### Regularization helps prevent a model from 'memorizing' random noise. This 'memorization' is also known as:
- [ ] Underfitting
- [x] Overfitting
- [ ] Global Optimization
- [ ] Standardization

### In the Ames modeling workflow, why was 'SalePrice' log-transformed (y = np.log)?
- [ ] To make the calculation of Alpha easier.
- [x] To handle right-skewness in the target variable.
- [ ] Because Lasso only works on log-scaled data.
- [ ] To increase the number of features in the model.

### Lasso Regression uses an L1 penalty. The '1' in L1 refers to:
- [ ] The fact that it only uses one feature.
- [x] Taking the absolute value to the power of 1.
- [ ] Having only one alpha value.
- [ ] Being the #1 preferred method for Ridge enthusiasts.

### If Alpha is set to an 'overwhelmingly high' value in Lasso, what is the likely result?
- [ ] Every feature will have a massive coefficient.
- [ ] The model will be identical to an OLS model.
- [x] The model will drop all predictors, leaving only the intercept.
- [ ] The R-squared value will increase significantly.

### When comparing Ridge and ElasticNet behavior on highly correlated groups of variables:
- [ ] Ridge kills the whole group; ElasticNet keeps one.
- [x] Ridge keeps the whole group; ElasticNet can keep the group but also zero out truly useless variables.
- [ ] Both models behave exactly like Forward Selection.
- [ ] Neither model can handle correlated variables.

### Which step is 'CRITICAL' for linear models when performing One-Hot Encoding to prevent multicollinearity?
- [ ] Scaling the target variable.
- [x] Setting 'drop_first=True'.
- [ ] Removing all numeric columns.
- [ ] Using cross-validation with 100 folds.

## 17_2_4_4: Hyperparameter Tuning

### In the manual alpha tuning loop, you plot both training R² and cross-validation R² against alpha. As alpha increases from 0.01 to 1000, what pattern do you expect to see in the two curves?
- [ ] Training R² rises steadily while CV R² falls steadily, reflecting increasing model complexity.
- [x] Training R² falls steadily while CV R² rises then falls, peaking at the optimal alpha.
- [ ] Both curves rise in parallel, since regularization always improves model fit.
- [ ] Training R² stays flat while CV R² oscillates randomly around a constant value.

### A colleague runs GridSearchCV with 5-fold CV on a training set of 2,000 samples, testing 10 alpha values. How many total model fits does GridSearchCV perform?
- [ ] 10, because it only fits one model per alpha value.
- [x] 50, because it fits 10 alphas across 5 folds.
- [ ] 2,000, because it fits one model per training sample.
- [ ] 15, because it adds the number of alphas and folds together.

### You use GridSearchCV with refit=True (the default). After the search completes, what model does grid_search.best_estimator_ return?
- [ ] The model trained on the single best-performing fold during the search.
- [x] The model retrained on the entire training set using the best hyperparameters found.
- [ ] An ensemble of all models tested during the search, weighted by their CV scores.
- [ ] The model from the fold with the lowest validation error, without retraining.

### Why does the Pipeline prefix hyperparameter keys with 'model__' (e.g., 'model__alpha') when passing them to GridSearchCV?
- [ ] It is a naming convention that GridSearchCV ignores; any prefix would work.
- [x] It tells GridSearchCV which step in the pipeline the parameter belongs to.
- [ ] It prevents GridSearchCV from trying to tune the scaler's parameters.
- [ ] It signals to scikit-learn that the parameter should be tuned on a logarithmic scale.

### You fit a StandardScaler on the full dataset, then pass the scaled data to cross_val_score with cv=5. What is the consequence?
- [ ] Nothing changes; scaling before CV is mathematically equivalent to scaling within each fold.
- [x] The scaler's mean and variance incorporate information from every fold, leaking into each training split.
- [ ] Cross-validation automatically detects the pre-scaled data and adjusts its fold boundaries.
- [ ] The model trains faster because the scaling computation is performed only once instead of five times.

### In ElasticNet, the l1_ratio parameter controls the mix between L1 and L2 penalties. What does l1_ratio=0.7 mean?
- [x] 70% of the penalty comes from the L1 (Lasso) term and 30% from the L2 (Ridge) term.
- [ ] 70% of the features will be dropped to exactly zero by the model.
- [ ] The model will behave 70% like Ridge and 30% like Lasso.
- [ ] The overall penalty strength is reduced to 70% of the specified alpha value.

### After tuning three regularized models (Ridge, Lasso, ElasticNet) with GridSearchCV, all three achieve nearly identical test R² scores. What is the most likely explanation?
- [ ] The hyperparameter grids were too narrow to distinguish between the models.
- [x] The remaining features are weakly informative, so all regularization approaches converge to similar solutions.
- [ ] GridSearchCV always returns the same model regardless of the algorithm specified.
- [ ] The test set is too small to distinguish between different modeling approaches.

### You run GridSearchCV on a training set and find the best alpha. You then evaluate that model on a held-out test set and get R² = 0.83. Why might this score be slightly inflated?
- [ ] The test set is systematically easier to predict than the training set due to random sampling.
- [x] The best alpha was selected because it happened to perform well on the specific CV folds, partially capturing noise.
- [ ] GridSearchCV uses a different scoring metric than the one used for the final test evaluation.
- [ ] The model was retrained on the full training set, which artificially boosts test performance.

### In nested cross-validation with 5 outer folds and 5 inner folds, how many total model fits are performed when searching over 8 alpha values?
- [ ] 40, because there are 5 outer folds times 8 alpha values, giving 40 total fits.
- [x] 200, because each outer fold runs the full inner GridSearch.
- [ ] 25, because there are 5 outer folds times 5 inner folds, giving 25 total fits.
- [ ] 13, because you add the 5 outer folds, 5 inner folds, and 3 unique alpha values together.

### In the nested CV code, cross_val_score is called with a GridSearchCV object as its estimator. What happens during each outer fold?
- [ ] cross_val_score extracts the best hyperparameters from the GridSearchCV and fits a simple model with them.
- [x] cross_val_score calls .fit() on the GridSearchCV, which runs the full inner search, then uses the refitted best model to predict on the outer test fold.
- [ ] cross_val_score skips the inner search and uses the default hyperparameters of the base estimator.
- [ ] cross_val_score averages the predictions of all hyperparameter combinations tested by the inner search.

### Your nested CV results show Ridge R² = 0.7910 ± 0.0266, while the single train/test split gave R² = 0.8300. What does the ± 0.0266 tell you?
- [x] The model's performance varies by about 2.7 percentage points depending on which data ends up in each fold.
- [ ] The nested CV code has a bug that introduces random noise into the scores.
- [ ] The inner loop's hyperparameter search is unstable and needs more alpha values.
- [ ] The outer test folds contain outlier houses that distort the average score.

### Why does nested CV use the full dataset (X, y) rather than just X_train, y_train?
- [x] Because nested CV replaces the train/test split; every data point serves as both training and test data across different folds.
- [ ] Because the outer loop needs more data than the inner loop to produce stable hyperparameter estimates.
- [ ] Because GridSearchCV requires the full dataset to properly scale features before splitting.
- [ ] Because using only the training set would cause the inner loop to overfit more severely.

### In nested CV, Outer Fold 1 selects alpha=1 as optimal, while Outer Fold 3 selects alpha=10. What does this tell you?
- [ ] The inner loop's random state was not fixed, causing non-reproducible results.
- [x] The optimal regularization strength depends on which specific samples are in the training subset.
- [ ] The model is fundamentally unstable and should not be trusted for any predictions.
- [ ] One of the outer folds contains corrupted data that skews the hyperparameter search.

### A learning curve shows training R² = 0.95 and validation R² = 0.70 at the largest training size, with a persistent gap between the two curves. What should you do?
- [ ] Add more training data, since the validation curve is still improving.
- [ ] Increase model complexity, since both scores are too low.
- [x] Increase regularization or reduce model complexity, since the model is overfitting.
- [ ] Switch to a different scoring metric, since R² is not appropriate for this dataset.

### A learning curve shows both training and validation R² converging at approximately 0.55, with the validation curve flatlining as training size increases. What does this indicate?
- [ ] The model is overfitting and would benefit from more training data.
- [x] The model is underfitting; adding more data will not help, and a more complex model is needed.
- [ ] The model is well-calibrated and performing at its theoretical maximum.
- [ ] The cross-validation folds are too small to produce reliable validation scores.

### In the learning curve code, train_sizes=np.linspace(0.1, 1.0, 5) is used. How many different training set sizes are evaluated?
- [x] 5, representing 10%, 32.5%, 55%, 77.5%, and 100% of the training data.
- [ ] 10, because linspace generates values from 0.1 to 1.0 in steps of 0.1.
- [ ] 1, because the model is only trained on the full dataset at the end.
- [ ] 3, because the first and last values are excluded from the evaluation.

### You compare nested CV R² scores for Ridge (0.7910), Lasso (0.7910), and ElasticNet (0.7909). All three are nearly identical. What does this suggest about the underlying data?
- [x] The feature set is small enough that L1 and L2 penalties produce nearly equivalent coefficient shrinkage.
- [ ] The GridSearchCV grids for all three models were identical, forcing them to the same solution.
- [ ] The nested CV implementation has a bug that returns the same score for all models.
- [ ] The data contains no predictive signal, so all models converge to the same baseline.

### Which statement best describes the relationship between the inner loop's best CV score and the outer loop's average score in nested CV?
- [x] The inner loop's score is always higher because it averages over smaller training subsets.
- [ ] The outer loop's score is always higher because it evaluates on larger test folds.
- [ ] The two scores are always identical because they use the same scoring metric.
- [ ] The inner loop's score is always lower because it uses more folds than the outer loop.

### You are building a production model for a client who needs a trustworthy performance estimate. Your dataset has 1,500 samples. Which approach should you use?
- [ ] A single 80/20 train/test split with GridSearchCV, because it is fast and simple.
- [x] Nested cross-validation, because it provides an unbiased estimate with quantifiable variance.
- [ ] Leave-one-out cross-validation without hyperparameter tuning, because it uses all data for training.
- [ ] A single model fit on all 1,500 samples with no validation, because the dataset is too small to split.

### In the bias-variance tradeoff plot for Ridge regression, the CV R² peaks at alpha=1. If you were to add alpha=0.5 and alpha=2 to your grid, what would you expect?
- [ ] Alpha=0.5 would have a higher CV R² because lower alpha always means better generalization.
- [ ] Alpha=2 would have a higher CV R² because higher alpha always means better generalization.
- [ ] Both would likely have lower CV R² than alpha=1, since alpha=1 is already at the peak.
- [x] One would be higher and one lower, but you cannot predict which without running the experiment.

### A student claims: 'Nested CV is unnecessary if I have a separate test set that I never touch during tuning.' What is the strongest counterargument?
- [ ] The student is correct; a separate test set completely eliminates the need for nested CV.
- [x] A single test set gives only one performance estimate with no measure of its variability across different data splits.
- [ ] Nested CV is always faster than a train/test split, so there is no reason not to use it.
- [ ] The test set score is always lower than the nested CV score, making nested CV the more conservative choice.

### When using learning_curve() with cv=5, how is the validation score computed at each training size?
- [ ] The model is trained once on the specified fraction of data and scored on the remaining data.
- [x] For each training size, 5 models are trained on different folds and the validation scores are averaged.
- [ ] The model is trained on the full dataset and scored on 5 different random subsets of the test set.
- [ ] The training and validation scores are computed on the same data to measure overfitting.

### You notice that the learning curve's validation score is still climbing at the largest training size (100% of training data). What is the most actionable next step?
- [ ] Reduce model complexity, since the model is clearly overfitting at all training sizes.
- [x] Collect more training data, since the model has not yet plateaued and could benefit from additional examples.
- [ ] Switch to a simpler model, since the current model is too complex for the available data.
- [ ] Increase the number of cross-validation folds to get a more accurate validation score.

### In the notebook, the 'Features Kept' column counts coefficients with absolute value > 1e-5. Why is this threshold used instead of checking for exact zeros?
- [x] Because Ridge regression never produces exact zeros; coefficients are shrunk toward zero but rarely reach it.
- [ ] Because floating-point arithmetic makes exact zero comparisons unreliable for all models.
- [ ] Because Lasso always produces coefficients slightly above zero due to numerical imprecision.
- [ ] Because the 1e-5 threshold is the default regularization strength in scikit-learn.

### You run nested CV with 5 outer folds and 3 inner folds, searching 12 hyperparameter combinations. Your manager asks how long it will take compared to a single GridSearchCV run (5 folds, 12 combinations). What do you tell them?
- [ ] It will take about the same time, since the inner loop reuses computations from the outer loop.
- [x] It will take roughly 5 times longer, because the outer loop runs the full inner GridSearch once per outer fold.
- [ ] It will take 15 times longer, because you multiply the outer folds, inner folds, and combinations.
- [ ] It will take less time, because nested CV uses smaller training sets in each fold.

### The notebook states that nested CV scores are 'typically lower' than train/test split scores. Under what circumstance might nested CV produce a higher score?
- [x] When the single test split happened to contain unusually difficult-to-predict samples by chance.
- [ ] When the inner loop uses more folds than the outer loop, inflating the score.
- [ ] When the model is severely underfitting, making all evaluation methods produce identical scores.
- [ ] This can never happen; nested CV always produces a lower score by definition.

### In the GridSearchCV output, 'Fitting 5 folds for each of 20 candidates, totalling 100 fits' appears for ElasticNet. What are the 20 candidates?
- [ ] 20 different random splits of the training data into train and validation sets.
- [x] The Cartesian product of 4 alpha values and 5 l1_ratio values, giving 20 unique hyperparameter combinations.
- [ ] 20 different model architectures, each with a different number of features.
- [ ] 20 iterations of the ElasticNet optimization algorithm before convergence.

### You fit a learning curve and observe that the training score starts at 0.99 with 10% of the data and drops to 0.79 with 100% of the data, while the validation score rises from 0.50 to 0.78. What is happening?
- [x] The model is overfitting on small training sets and gradually improving its generalization as more data is added.
- [ ] The model is underfitting at all training sizes and would benefit from a more complex architecture.
- [ ] The cross-validation folds are too small at low training sizes, producing unreliable scores.
- [ ] The learning rate is too high, causing the model to diverge as more data is introduced.

### A colleague argues that nested CV is 'wasteful' because it trains hundreds of models but only reports a single average score. How would you respond?
- [ ] They are correct; a single train/test split with GridSearchCV is always sufficient for reporting results.
- [x] The average score is the deliverable, but the standard deviation across outer folds is equally valuable information about model stability.
- [ ] Nested CV produces a separate final model for each outer fold, giving you five models to deploy in production.
- [ ] The computational cost of nested CV is negligible on modern hardware, so the objection is moot.

### After running nested CV, you want to deploy a final model for production predictions. Which model should you deploy?
- [ ] The average of all models produced across the outer folds, since averaging reduces variance.
- [ ] The model from the outer fold with the highest test score, since it performed best.
- [x] A fresh model trained on all data using the most common hyperparameters from the inner loops.
- [ ] The model from the inner loop with the lowest validation error, since it was most accurate.

## 17_2_4_5: Tree-Based Methods

### Linear models required manual polynomial features to capture non-linear patterns. How do tree-based models handle non-linearity?
- [ ] They apply logarithmic transformations to all features automatically.
- [x] They partition the data into regions and predict the average within each region.
- [ ] They add squared terms to the regression equation during training.
- [ ] They use gradient descent to find the optimal non-linear curve.

### In a regression tree, what value does each leaf node predict?
- [ ] The median of the target values of training samples in that leaf.
- [x] The average of the target values of training samples in that leaf.
- [ ] The most frequent target value among training samples in that leaf.
- [ ] A weighted sum of coefficients applied to the features in that leaf.

### How does a regression tree decide which feature and split point to use at each node?
- [ ] It randomly selects a feature and tries all possible split points on that feature.
- [ ] It selects the feature with the highest correlation to the target variable.
- [x] It tries every possible split on every feature and picks the one that reduces error the most.
- [ ] It uses the feature that was most important in the parent node's split.

### An unconstrained decision tree achieves R² = 1.0 on training data but R² = 0.80 on test data. What is happening?
- [ ] The model is underfitting because it has too few splits to capture the patterns.
- [x] The model is overfitting by creating a separate leaf for nearly every training sample.
- [ ] The training data contains outliers that the model is correctly ignoring.
- [ ] The test set is fundamentally different from the training set due to data leakage.

### In the depth experiment, the test R² peaked at depth 7 and then declined as depth increased. What does this pattern represent?
- [x] The bias-variance tradeoff, where deeper trees reduce bias but increase variance.
- [ ] A computational limitation where deeper trees run out of memory.
- [ ] The learning rate decay, where deeper trees contribute less to the prediction.
- [ ] A data quality issue where deeper splits encounter noisier samples.

### Why does a Random Forest use bootstrap sampling (drawing with replacement) for each tree?
- [ ] To ensure every tree sees the exact same data but in a different order.
- [ ] To reduce the computational cost of training each individual tree.
- [x] To give each tree a different perspective on the data so they make different errors.
- [ ] To eliminate the need for a separate test set during model evaluation.

### In a Random Forest, what does max_features='sqrt' mean at each split?
- [ ] Each tree considers only the square root of the total features across the entire tree.
- [x] Each tree considers a random subset of about √n features at each individual node.
- [ ] The forest builds exactly √n trees, where n is the number of features.
- [ ] Each tree uses the top √n most important features and ignores the rest.

### Why does feature randomness in Random Forests prevent every tree from splitting on the same dominant feature first?
- [ ] Because the dominant feature is removed from the dataset before training begins.
- [x] Because each tree is forced to consider only a random subset of features at each node.
- [ ] Because the dominant feature's values are randomly shuffled before each split.
- [ ] Because the algorithm penalizes trees that use the same feature twice.

### The Random Forest selected max_depth=None as optimal. Why is unlimited depth acceptable in a forest but catastrophic for a single tree?
- [ ] Because the forest uses a different splitting criterion that prevents overfitting.
- [x] Because the averaging process across diverse trees cancels out individual overfitting.
- [ ] Because each tree in the forest sees fewer features, making deep trees simpler.
- [ ] Because the forest automatically prunes trees that show signs of overfitting.

### What is the primary objective of a Random Forest from a bias-variance perspective?
- [ ] Reducing bias by building deeper and deeper trees sequentially.
- [x] Reducing variance by averaging many diverse, uncorrelated predictions.
- [ ] Reducing both bias and variance equally through random feature selection.
- [ ] Eliminating bias entirely by using an infinite number of trees.

### In gradient boosting, what does each new tree predict?
- [ ] The original target variable (e.g., house price) directly.
- [x] The residuals (errors) left by the ensemble of all previous trees.
- [ ] The average of all previous trees' predictions.
- [ ] A weighted combination of the target variable and the previous residuals.

### Gradient boosting starts with an initial prediction equal to the average target value. What is the role of the first tree?
- [ ] It predicts the target variable directly, ignoring the initial prediction.
- [x] It predicts the residuals from the initial prediction, correcting the average.
- [ ] It replaces the initial prediction entirely with its own predictions.
- [ ] It determines which features are most important for all subsequent trees.

### What is the key difference between Random Forests and Gradient Boosting in how they build their ensembles?
- [ ] Random Forests build trees sequentially; Gradient Boosting builds them in parallel.
- [x] Random Forests average independent trees; Gradient Boosting corrects errors sequentially.
- [ ] Random Forests use deep trees; Gradient Boosting uses only single-split trees.
- [ ] Random Forests reduce bias; Gradient Boosting reduces variance.

### Why does HistGradientBoostingRegressor use histogram binning?
- [ ] To convert categorical features into numeric codes automatically.
- [x] To group continuous data into discrete bins, speeding up the split-finding process.
- [ ] To reduce the number of features by combining correlated ones into bins.
- [ ] To handle missing values by placing them into a separate histogram bin.

### The raw-data HGB model achieved R² = 0.9449, nearly identical to the cleaned-data version. What does this demonstrate?
- [ ] That data cleaning is never necessary when using tree-based models.
- [x] That tree-based models are robust to messy data and don't require extensive preprocessing.
- [ ] That the Ames dataset was already clean enough that no cleaning was needed.
- [ ] That HGB automatically performs the same cleaning steps as the notebook's cleaning function.

### How does XGBoost's built-in regularization differ from Ridge and Lasso regularization?
- [x] XGBoost applies penalties to leaf weights and tree complexity, not to linear coefficients.
- [ ] XGBoost only uses L1 regularization, while Ridge and Lasso use both L1 and L2.
- [ ] XGBoost applies regularization after training, while Ridge and Lasso apply it during training.
- [ ] XGBoost does not use regularization; it relies solely on tree depth limits.

### Boosting is inherently sequential, making it hard to parallelize. How does XGBoost achieve speed improvements?
- [ ] It builds multiple trees in parallel and averages their predictions.
- [x] It parallelizes the internal work of building each tree, such as feature sorting.
- [ ] It skips the residual calculation step for trees after the first 50.
- [ ] It uses a single CPU core but with a more efficient optimization algorithm.

### How does XGBoost handle missing values at each split?
- [ ] It imputes missing values with the median before training begins.
- [ ] It removes any sample with a missing value from the training set.
- [x] It tests sending missing values left or right and learns which direction reduces error.
- [ ] It assigns missing values to a separate leaf node that predicts the global average.

### Feature importance in tree models measures how much each split reduced prediction error. What is a key limitation of this metric?
- [ ] It cannot handle categorical features, so their importance is always zero.
- [x] It tells you what the model prioritizes but not how the feature affects the target.
- [ ] It only works for the top 5 features and ignores all remaining features.
- [ ] It requires the model to be retrained after importance scores are calculated.

### In the feature importance plot, Overall Qual accounts for 45.76% of the model's learning. What does this percentage mean?
- [ ] Overall Qual alone can predict 45.76% of house prices without any other features.
- [x] Splits on Overall Qual were responsible for 45.76% of the total error reduction across all trees.
- [ ] 45.76% of the trees in the ensemble used Overall Qual as their first split.
- [ ] Overall Qual has a coefficient of 0.4576 in the underlying linear approximation.

### Gr Liv Area has a VIF of 118 (extreme multicollinearity) but only 2.98% feature importance. Why?
- [ ] Living area is genuinely unimportant for predicting house prices in Ames.
- [x] XGBoost chose correlated features like Overall Qual for splits, leaving Gr Liv Area with little credit.
- [ ] The VIF calculation is only relevant for linear models and has no bearing on tree models.
- [ ] The feature importance calculation is biased against continuous features.

### In the model comparison, XGBoost's single-split score was 0.9418 but its nested CV estimate was 0.9177. What explains this gap?
- [ ] The nested CV used a different scoring metric that is inherently more conservative.
- [x] The single-split score was inflated because the same data influenced both tuning and evaluation.
- [ ] The nested CV used fewer trees, resulting in a less powerful model.
- [ ] The test set in the single-split approach contained easier-to-predict houses by chance.

### The nested CV standard deviation for XGBoost was ±0.0195. What does this tell you?
- [x] The model's performance varies by about 2 percentage points across different data splits.
- [ ] The hyperparameter search found 19.5% of the possible combinations to be optimal.
- [ ] The model is overfitting by approximately 1.95 percentage points on each fold.
- [ ] The inner loop's best score was 1.95 percentage points higher than the outer loop's score.

### The notebook recommends starting with a Random Forest for most real-world problems. Why?
- [ ] Random Forests always produce the highest accuracy of any model.
- [x] Random Forests are fast, robust, and hard to mess up compared to tuning boosting models.
- [ ] Random Forests are the only tree-based method that handles missing values natively.
- [ ] Random Forests provide the most interpretable feature importance scores.

### What tradeoff do you accept when moving from linear regression to XGBoost?
- [ ] You gain interpretability but lose the ability to handle non-linear patterns.
- [x] You gain predictive accuracy but lose the ability to explain individual coefficient effects.
- [ ] You gain speed but lose the ability to handle categorical features.
- [ ] You gain robustness to outliers but lose the ability to use cross-validation.

### In the nested CV for XGBoost, the inner loop used 3 folds and the outer loop used 5 folds with 12 hyperparameter combinations. How many total model fits occurred?
- [ ] 60, because 5 outer folds times 12 hyperparameter combinations equals 60.
- [x] 180, because 5 outer folds each run a full inner search of 12 combinations times 3 inner folds.
- [ ] 15, because 5 outer folds times 3 inner folds equals 15 total evaluations.
- [ ] 36, because 12 hyperparameter combinations times 3 inner folds equals 36.

### Central Air achieved 7.86% feature importance despite being a binary feature. Why might a binary feature score highly?
- [ ] Binary features are always more important than continuous features in tree models.
- [x] The presence or absence of central air is a major price differentiator in the Ames market.
- [ ] Binary features are used more frequently as split points because they are easier to compute.
- [ ] The model automatically upweights binary features to compensate for their simplicity.

### The notebook mentions that
- [ ] They identify which features should be removed from the model entirely.
- [x] They reveal the direction and shape of a feature's effect on the prediction.
- [ ] They calculate the exact dollar value contribution of each feature.
- [ ] They determine the optimal hyperparameters for the model.

### Decision trees convert categorical features to numeric codes using .cat.codes. What risk does this introduce?
- [x] The model treats categories as having an ordinal relationship that may not exist.
- [ ] The model loses all information about the categorical feature during conversion.
- [ ] The model becomes unable to handle new categories that appear in the test set.
- [ ] The model requires one-hot encoding to work correctly with numeric codes.

### The notebook's final model comparison shows Decision Tree (0.83) < Random Forest (0.88) < HGB (0.94) < XGBoost (0.94). What does this progression illustrate?
- [ ] That more complex models always produce better results regardless of the dataset.
- [x] That each ensemble method builds on the previous one's weaknesses to improve accuracy.
- [ ] That the differences between models are negligible and any tree model would suffice.
- [ ] That the hyperparameter grids were progressively larger, giving later models an unfair advantage.
